---
title: Setup tailwindcss with nextjs superfast
date: 2021/3/26
description: Learn awesomeness..
tag: web development
author: You
---

import Prism from 'components/Prism'

# Setup tailwindcss with nextjs superfast

```
npx create-next-app my-tailwind-setup
cd my-tailwind-setup

npm install -D tailwindcss@latest postcss@latest autoprefixer@latest
npx tailwindcss init -p #Creates a 'tailwind.config.css' file.
```

Src: https://github.com/vercel/next.js/tree/canary/examples/with-tailwindcss

WHAT🧸︎ TO DO NOW ?

1.  🔽︎ Open `_app.js` file, remove import of `globals.css` and add below one::

<Prism code={`

    import 'tailwindcss/tailwind.css'

`} />

2.  🔽︎ Specify below in 'tailwind.config.css' file, for efficient tailwindcss experience::

<Prism code={`
    mode: 'jit',
    purge: ['./pages/**/*.{js,ts,jsx,tsx}', './components/**/*.{js,ts,jsx,tsx}'],

`}/>

3.  🔽︎ 🧪︎ TEST IF TAILWIND WORKS BY deleting everything in `index.js` file and using below instead::
    import Head from 'next/head'

<Prism code={`
import Head from 'next/head'

export default function Home() {
  return (
    <div>
      <Head>
        <title>My next app with tailwindcss</title>
        <meta name='description' content='Generated by create next app' />
        <link rel='icon' href='/favicon.ico' />
      </Head>

      <main>
        <div className='text-center text-blue-500'>justify-center and blue text.</div>
      </main>

      <footer></footer>
    </div>
  )
}
`} />
