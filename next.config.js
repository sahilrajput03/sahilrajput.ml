const withNextra = require('nextra')('nextra-theme-blog', './theme.config.js')
module.exports = withNextra()
// react-prism-renderer: className, children and metastring.
// className = to speficy sytax highlights
// children = code
// metastring = a string representation of everything following the backticks and language

// parse-numeric-range, yarn add parse-numeric-range
